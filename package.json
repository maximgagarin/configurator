{
  "scripts": {
    "dev": "vite",
    "deploy": "vite build && gh-pages -d dist",
    "build": "vite build"
  },
  "dependencies": {
    "gsap": "^3.12.5",
    "three": "^0.170.0",
    "vite": "^5.4.11"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}
